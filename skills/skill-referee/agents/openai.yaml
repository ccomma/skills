interface:
  display_name: "Skill Referee"
  short_description: "Referee skill overlaps and routing rules."
  default_prompt: "Use $skill-referee to check available skills in any domain for overlap, conflicts, trigger ambiguity, or convention mismatches. It will ask fixed-scope questions, inspect metadata first, then deep-read only confirmed skills."
