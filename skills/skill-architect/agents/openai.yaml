interface:
  display_name: "Skill Architect"
  short_description: "Lock a skill's kernel before designing its structure."
  default_prompt: "Use $skill-architect when one skill's boundary is already clear and you need to lock the kernel first: produce the kernel sentence, trigger boundary, first move, strongest thing, and wrong-scope sentence before deciding expansion, sections, references, scripts, or bundle structure."
